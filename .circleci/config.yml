version: 2
jobs:
  build:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  development:
    requires:
      - build
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  production:
    requires:
      - build
      - development
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test


workflows:
  version: 2
  build_accept_deploy:
    jobs:
      - build
      - development
      - production
version: 2
jobs:
  test:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  development:
    docker:
      - image: circleci/node:10.15.3
    filters:
      branches:
        only: development
    steps:
      - checkout
      - run: npm install
      - run: npm test
  # production:
  #   # branches:
  #   #   only:
  #   #     - master
  #   docker:
  #     - image: circleci/node:10.15.3
  #   steps:
  #     - checkout
  #     - run: npm install
  #     - run: npm test


workflows:
  version: 2
  build:
    jobs:
      - test
      - development
      # - production
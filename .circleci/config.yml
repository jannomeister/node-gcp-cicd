version: 2
jobs:
  test:
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  dev:
    docker:
      - image: circleci/node:10.15.3
    branches:
      only:
        - development
    steps:
      - checkout
      - run: npm install
      - run: npm test
  prod:
    docker:
      - image: circleci/node:10.15.3
    branches:
      only:
        - master
    steps:
      - checkout
      - run: npm install
      - run: npm test


workflows:
  version: 2
  build:
    jobs:
      - test
      - dev
      - prod
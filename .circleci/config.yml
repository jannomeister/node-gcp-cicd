version: 2
jobs:
  test:
    # branches:
    #   only:
    #     - master
    #     - development
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  development:
    # branches:
    #   only:
    #     - development
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  production:
    # branches:
    #   only:
    #     - master
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test


workflows:
  version: 2
  build:
    jobs:
      - test
      - development
      - production
version: 2
jobs:
  one:
    # branches:
    #   only:
    #     - master
    #     - development
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  two:
    # branches:
    #   only:
    #     - development
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test
  three:
    # branches:
    #   only:
    #     - master
    docker:
      - image: circleci/node:10.15.3
    steps:
      - checkout
      - run: npm install
      - run: npm test


workflows:
  version: 2
  build:
    jobs:
      - one
      - two
      - three